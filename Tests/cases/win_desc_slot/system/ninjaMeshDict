domain
{
    min (15 -3.5 72);
    max (21 4 78);
    n   (12 15 12);
}

patches
{
    xmin { type patch; name inlet; }
    xmax { type patch; name outlet; }
    ymin { type patch; name symmetry; }
    ymax { type patch; name symmetry; }
    zmin { type patch; name symmetry; }
    zmax { type patch; name atmosphere; }
}

geometry
{
    descargadorG4bT.stl { name fixedWalls; }
    aireadorT.stl       { name aireador;   }
}

locationInMesh (15.06 0.25 77);

refinement
{
    worksBox   { type box; min (-2.0 -3.5 70.0); max (42.0 4.0 92.0); level 1; }
    nearWalls  { type surface; stl descargadorG4bT.stl; distance 0.4; level 2; }
    conduitBox { type box; min (-1.0 -3.5 73.0); max (40.0 4.0 81.5); level 1; }
    nearAir    { type surface; stl aireadorT.stl; distance 0.5; level 2; }
}

layers
{
    descargadorG4bT.stl { nLayers 4; expansionRatio 1.5; finalLayerThickness 0.3; }
}

