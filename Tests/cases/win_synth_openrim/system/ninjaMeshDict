domain
{
    min (-5 -1 -1);
    max (3 1 1);
    n   (8 2 2);
}

patches
{
    xmin { type patch; name xmin; }
    xmax { type patch; name xmax; }
    ymin { type patch; name ymin; }
    ymax { type patch; name ymax; }
    zmin { type patch; name zmin; }
    zmax { type patch; name zmax; }
}

geometry
{
    openbox.stl { name openbox; }
}

locationInMesh (-4 -0.9 -0.9);
